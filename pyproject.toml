[tool.poetry]
name = "api-spec-converter"
version = "0.1.0"
description = ""
authors = ["Javier Arredondo <javier.arredondo.contreras@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
